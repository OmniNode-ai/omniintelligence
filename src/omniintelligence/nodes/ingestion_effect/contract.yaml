---
# ONEX Node Contract - Ingestion Effect Node (STUB)
#
# STATUS: STUB - Minimal contract for forward compatibility
# This node will handle document ingestion effects when implemented.
# =============================================================================
contract_version:
  major: 1
  minor: 0
  patch: 0
node_version:
  major: 1
  minor: 0
  patch: 0
name: "ingestion_effect"
node_type: "EFFECT_GENERIC"
status: "stub"
description: >-
  STUB: Effect node for document ingestion operations. Will handle file reading, format detection, and
  preprocessing effects.
input_model:
  name: "ModelIngestionInput"
  module: "omniintelligence.nodes.ingestion_effect.models"
  description: "STUB: Input containing source_path and options for ingestion."
output_model:
  name: "ModelIngestionOutput"
  module: "omniintelligence.nodes.ingestion_effect.models"
  description: "STUB: Output containing ingested content and metadata."
# Minimal sections for stub
dependencies: []
# Error Handling Configuration (stub - minimal)
# =============================================
error_handling:
  error_types:
    - name: "IngestionValidationError"
      description: "Input validation failed (e.g., invalid source path)"
      recoverable: false
      retry_strategy: "none"
    - name: "IngestionEffectError"
      description: "Error during file ingestion"
      recoverable: true
      retry_strategy: "exponential_backoff"
    - name: "FileNotFoundError"
      description: "Source file not found"
      recoverable: false
      retry_strategy: "none"
health_check:
  enabled: false
  endpoint: "/health"
  interval_seconds: 30
metadata:
  author: "OmniNode Team"
  created: "2025-11-14"
  updated: "2026-01-18"
  status: "stub"
  tags:
    - "ONEX"
    - "effect"
    - "ingestion"
    - "stub"
