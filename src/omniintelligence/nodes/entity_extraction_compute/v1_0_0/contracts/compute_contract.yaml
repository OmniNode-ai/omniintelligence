name: entity_extraction_compute
version:
  major: 1
  minor: 0
  patch: 0

description: |
  Compute node for extracting entities from code and documents.
  Identifies classes, functions, modules, variables, and other code entities
  using AST parsing for multiple programming languages.

node_type: compute

input_model: omniintelligence.models.ModelEntityExtractionInput
output_model: omniintelligence.models.ModelEntityExtractionOutput

algorithm:
  algorithm_type: entity_extraction
  factors:
    extraction:
      weight: 1.0
      calculation_method: ast_parsing

performance:
  single_operation_max_ms: 5000

dependencies: []
