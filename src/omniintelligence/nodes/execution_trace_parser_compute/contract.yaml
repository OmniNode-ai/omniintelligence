# ONEX Node Contract - Execution Trace Parser Compute Node
#
# Part of pattern_extraction split (Track 2 Intelligence Hook System)
contract_version:
  major: 1
  minor: 0
  patch: 0
node_version: "1.0.0"
name: "execution_trace_parser_compute"
node_type: "COMPUTE_GENERIC"
description: "Compute node for parsing execution traces from PostgreSQL intelligence tracking system. Extracts trace events, timing, and error information from structured trace data."
input_model:
  name: "ModelTraceParsingInput"
  module: "omniintelligence.nodes.execution_trace_parser_compute.models"
  description: "Input containing trace data, format specification, and extraction options."
output_model:
  name: "ModelTraceParsingOutput"
  module: "omniintelligence.nodes.execution_trace_parser_compute.models"
  description: "Output containing parsed trace events and metadata."
operations:
  - name: "parse_trace"
    description: "Parse execution trace data"
    input_fields:
      - trace_data
      - correlation_id
      - trace_format
      - extract_errors
      - extract_timing
    output_fields:
      - success
      - parsed_events
      - error_events
      - timing_data
dependencies:
  - name: "trace_parser"
    type: "protocol"
    module: "omniintelligence.protocols"
    description: "Trace parsing engine"
health_check:
  enabled: true
  endpoint: "/health"
  interval_seconds: 30
metadata:
  author: "OmniNode Team"
  created: "2025-10-02"
  updated: "2026-01-18"
  tags:
    - "compute"
    - "trace"
    - "parser"
    - "pattern-extraction"
