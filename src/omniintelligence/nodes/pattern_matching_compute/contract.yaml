# ONEX Node Contract - Pattern Matching Compute Node
contract_version:
  major: 1
  minor: 0
  patch: 0
node_version: "1.0.0"
name: "pattern_matching_compute"
node_type: "COMPUTE_GENERIC"
description: "Compute node for matching code patterns against a pattern library. Identifies design patterns, anti-patterns, and custom patterns."
input_model:
  name: "ModelPatternMatchingInput"
  module: "omniintelligence.nodes.pattern_matching_compute.models"
  description: "Input containing code to match and pattern library reference."
output_model:
  name: "ModelPatternMatchingOutput"
  module: "omniintelligence.nodes.pattern_matching_compute.models"
  description: "Output containing matched patterns and confidence scores."
operations:
  - name: "match_patterns"
    description: "Match patterns in code"
    input_fields:
      - code_snippet
      - project_name
      - context
    output_fields:
      - success
      - patterns_matched
      - pattern_scores
dependencies:
  - name: "pattern_library"
    type: "protocol"
    module: "omniintelligence.protocols"
    description: "Pattern library for matching"
health_check:
  enabled: true
  endpoint: "/health"
  interval_seconds: 30
metadata:
  author: "OmniNode Team"
  created: "2025-11-14"
  updated: "2026-01-18"
  tags:
    - "compute"
    - "patterns"
    - "matching"
